# rust mockito example

An example of how to write integration tests for a web-service, which makes calls to external sites using [mockito](https://github.com/lipanski/mockito)

Run Tests with:

```bash
cargo test
```

Run with:

```bash
cargo run
```
